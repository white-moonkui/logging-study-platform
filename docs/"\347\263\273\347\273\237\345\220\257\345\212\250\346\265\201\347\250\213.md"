# 测井培训系统 - 启动流程

**版本**: v1.0
**日期**: 2026年3月6日

---

## 🚀 快速启动（5分钟）

### 方式一：一键启动（推荐）

```bash
# 在项目根目录 F:\ai\logging-study-platform-1

# 1. 一键启动
npm start

# 2. 访问系统
# http://localhost:3000
```

**预期效果**：

- ✅ 数据库自动连接
- ✅ 系统自动启动
- ✅ 显示欢迎页面
- ✅ 管理员登录功能

---

## 📋 详细启动步骤

### 第一步：环境准备（首次使用）

#### 1.1 检查Node.js环境

```bash
# 检查Node.js版本
node -v

# 检查npm版本
npm -v

# 要求：Node.js >= 16.0
```

如果版本低于16.0，需要先安装：

- 下载地址：https://nodejs.org/
- 选择 LTS 版本（推荐16.x或18.x）

#### 1.2 检查MongoDB环境

```bash
# 检查MongoDB是否安装
mongod --version

# 检查MongoDB服务状态（Windows）
netstat -ano | findstr :27017
```

如果MongoDB未安装：

- Windows：下载安装MongoDB Community Server
- 下载地址：https://www.mongodb.com/try/download/community
- 安装后启动MongoDB服务

#### 1.3 检查项目依赖

```bash
# 在项目目录运行
cd F:\ai\logging-study-platform-1

# 检查依赖是否已安装
npm install

# 如果提示依赖已存在，说明已经安装过了
```

---

### 第二步：数据库初始化（首次使用）

#### 2.1 初始化数据库（可选）

```bash
# 初始化数据库，导入示例数据
npm run init-db

# 验证数据是否导入成功
npm run verify-db
```

**预期输出**：

```
开始初始化数据库...
📡 连接数据库...
✅ 数据库连接成功
📝 数据库为空，开始初始化示例数据...
创建示例用户...
✅ 用户 admin 创建成功
✅ 用户 instructor 创建成功
✅ 用户 student 创建成功
创建示例知识内容...
✅ 知识内容 "自然伽马测井原理" 创建成功
✅ 知识内容 "电阻率测井基础" 创建成功
✅ 知识内容 "声波测井技术" 创建成功
✅ 知识内容 "API测井标准规范" 创建成功
✅ 知识内容 "测井-地质协同解释" 创建成功
创建示例案例...
✅ 案例 "复杂储层测井解释案例" 创建成功
✅ 案例 "钻井液侵入影响校正案例" 创建成功
创建示例考试...
✅ 考试 "测井基础理论考试" 创建成功
✅ 考试 "测井安全操作考试" 创建成功

数据库初始化完成！

验证数据库:
用户数量: 3
知识内容数量: 5
案例数量: 2
考试数量: 2
```

**注意**：

- 如果数据库已经有数据，系统会跳过初始化步骤
- 示例数据可以随时重新初始化（会覆盖现有数据）

---

### 第三步：启动应用服务器

#### 3.1 启动系统

```bash
# 方式1：使用启动脚本（推荐）
npm start

# 方式2：直接启动
node start.js

# 方式3：开发模式（自动重启）
npm run dev
```

**预期输出**：

```
==================================================
测井专业智能培训系统启动中...
==================================================
📡 连接数据库...
✅ 数据库连接成功
🔍 检查数据库状态...
✅ 数据库已有 3 个用户，跳过初始化
🤖 初始化AI服务...
✅ AI服务初始化完成，当前使用: 本地AI服务（规则引擎模式）
📊 初始化向量队列...
✅ 向量队列调度器已启动
🌐 启动Web服务器...
✅ Web服务器启动在端口 3000
==================================================
🎉 系统启动成功！
📱 访问地址: http://localhost:3000
📊 数据库: mongodb://127.0.0.1:27017/well_logging_training_memory
🤖 AI服务: 本地AI
==================================================
📋 默认账户:
   管理员: admin / admin123
   教师: instructor / instructor123
   学员: student / student123
==================================================
```

**注意**：

- 如果MongoDB连接失败，系统会自动降级到内存数据库模式
- AI服务会自动降级到本地规则引擎模式
- 向量队列会在后台启动

---

### 第四步：访问系统

#### 4.1 打开浏览器

访问以下地址之一：

- **http://localhost:3000**
- **http://127.0.0.1:3000**

#### 4.2 查看首页

首页会自动重定向到：`/prototype_v6_user_mgmt.html`

#### 4.3 管理员登录

1. 点击右上角"登录"按钮
2. 输入管理员账户：
    - 用户名：`admin`
    - 密码：`admin123`
3. 点击"登录"

**登录后可以看到**：

- ✅ 用户管理功能
- ✅ 知识库管理功能
- ✅ 案例管理功能
- ✅ 考试管理功能
- ✅ 系统统计信息

---

## 📱 功能测试

### 测试1：管理员登录

```
操作步骤：
1. 访问 http://localhost:3000
2. 点击右上角"登录"
3. 输入 admin / admin123
4. 点击"登录"

预期结果：
✅ 成功登录
✅ 进入管理员仪表盘
✅ 显示系统统计数据
```

### 测试2：查看知识库

```
操作步骤：
1. 登录后点击"知识库管理"
2. 查看已导入的知识内容

预期结果：
✅ 看到5个知识条目
✅ 包括：自然伽马测井原理、电阻率测井基础等
✅ 可以搜索、筛选、查看详情
```

### 测试3：查看交互式案例

```
操作步骤：
1. 登录后点击"案例学习"
2. 选择"复杂碳酸盐岩储层测井识别与分析"

预期结果：
✅ 查看案例详情
✅ 显示6步交互式学习
✅ 可以一步步进行学习
✅ 系统会给出反馈
```

### 测试4：完成考试

```
操作步骤：
1. 登录后点击"考试管理"
2. 选择"测井基础理论考试"
3. 开始考试
4. 完成所有题目

预期结果：
✅ 成功开始考试
✅ 可以查看和完成所有题目
✅ 提交后显示分数
✅ 查看详细解析
```

---

## 🔧 开发模式启动

如果需要开发模式（代码修改自动重启）：

```bash
# 启动开发模式
npm run dev

# 这会使用 nodemon 自动重启服务器
# 修改代码后自动重启，无需手动重启
```

---

## 🛠️ 常用命令

### 启动命令

```bash
npm start              # 生产启动
npm run dev            # 开发模式（自动重启）
node start.js          # 直接启动
node server.js         # 启动服务器（不启动应用初始化）
```

### 数据库命令

```bash
npm run init-db        # 初始化数据库
npm run verify-db      # 验证数据库
```

### 测试命令

```bash
npm test               # 运行测试
npm run test-jest      # Jest单元测试
npm run test-jest -- --watch  # 监听模式
npm run test-jest -- --coverage  # 覆盖率报告
```

### 代码质量命令

```bash
npm run lint           # ESLint检查
npm run lint -- --fix  # 自动修复
npm run format         # Prettier格式化
npm run format:check   # 格式检查
npm run gate           # 综合检查（lint + format）
```

---

## 🐛 常见问题

### 问题1：端口被占用

**错误信息**：

```
Error: listen EADDRINUSE: address already in use :::3000
```

**解决方案**：

```bash
# Windows：查找并关闭占用3000端口的进程
netstat -ano | findstr :3000
taskkill /PID <进程ID> /F

# 或者修改端口
# 编辑 .env 文件
PORT=3001
```

### 问题2：MongoDB连接失败

**错误信息**：

```
MongoServerSelectionError: connect ECONNREFUSED 127.0.0.1:27017
```

**解决方案**：

```bash
# 1. 检查MongoDB是否安装
mongod --version

# 2. 检查MongoDB服务是否启动
# Windows：
net start MongoDB

# Linux/Mac：
sudo systemctl start mongod

# 3. 重新启动系统
npm start
```

**降级方案**：

- 系统会自动降级到内存数据库模式
- 所有功能仍可正常使用
- 只是数据不会持久化到MongoDB

### 问题3：依赖未安装

**错误信息**：

```
Error: Cannot find module 'express'
```

**解决方案**：

```bash
# 重新安装依赖
npm install
```

### 问题4：权限问题

**错误信息**：

```
Error: EACCES: permission denied
```

**解决方案**：

```bash
# 修改文件权限
# Windows：右键文件 -> 属性 -> 安全 -> 编辑 -> 修改权限
```

### 问题5：环境变量未加载

**错误信息**：

```
Error: MONGODB_URI is not defined
```

**解决方案**：

```bash
# 检查 .env 文件是否存在
# 如果不存在，创建 .env 文件
# 或者系统会使用默认值
```

---

## 📊 系统状态检查

### 检查数据库连接

访问系统API：

```
http://localhost:3000/api/users
```

预期响应：

```json
{
  "success": true,
  "message": "获取用户列表成功",
  "data": {
    "users": [...],
    "total": 3
  }
}
```

### 检查健康状态

在浏览器控制台输入：

```javascript
fetch('http://localhost:3000/api/users')
    .then(res => res.json())
    .then(data => console.log(data))
    .catch(err => console.error(err));
```

---

## 🎯 功能清单

### 已实现的功能

#### 管理员功能

- ✅ 用户管理（查看、编辑、删除）
- ✅ 知识库管理（创建、编辑、删除、发布）
- ✅ 案例管理（创建、编辑、发布、审核）
- ✅ 考试管理（创建、编辑、发布、评分）
- ✅ 系统统计（数据仪表盘）

#### 用户功能

- ✅ 登录/注册
- ✅ 查看知识库
- ✅ 学习交互式案例
- ✅ 参加考试
- ✅ 查看学习进度
- ✅ 查看考试成绩

#### 教师功能

- ✅ 管理知识库
- ✅ 创建案例
- ✅ 创建考试
- ✅ 评分和反馈

#### 前端功能

- ✅ 响应式设计
- ✅ 多角色界面
- ✅ 交互式案例学习
- ✅ PDF/PPT查看器
- ✅ 视频播放器
- ✅ 知识卡片组件
- ✅ 虚拟列表

#### 后端功能

- ✅ 用户认证（JWT）
- ✅ 权限控制（角色）
- ✅ API路由（12个模块）
- ✅ 数据库适配器（支持内存和MongoDB）
- ✅ AI服务（本地+外部）
- ✅ 向量搜索服务

---

## 📝 使用建议

### 首次使用

1. **创建管理员账户**
    - 使用默认 admin/admin123
    - 登录后立即修改密码

2. **查看系统数据**
    - 查看知识库（5个示例）
    - 查看案例（3个交互式案例）
    - 查看考试（2套测试）

3. **测试核心功能**
    - 登录功能
    - 知识库浏览
    - 案例学习
    - 考试系统

### 后续使用

1. **导入培训材料**
    - 准备数据文件
    - 使用批量导入工具
    - 验证导入结果

2. **用户管理**
    - 创建不同角色用户
    - 分配权限
    - 监控使用情况

3. **内容管理**
    - 定期更新知识库
    - 补充新案例
    - 添加新考试

---

## 🔄 重启系统

### 重新启动

```bash
# 如果系统正在运行，先停止（Ctrl + C）

# 然后重新启动
npm start
```

### 重启特定服务

```bash
# 重启MongoDB（如果需要）
# Windows：
net stop MongoDB
net start MongoDB

# 重启AI服务（如果需要）
# 系统会在启动时自动初始化
```

---

## 📞 获取帮助

### 查看日志

```bash
# 系统日志会在控制台输出
# 包括：
# - 启动信息
# - 数据库连接状态
# - AI服务状态
# - 错误和警告
```

### 查看文档

```bash
# 查看项目README
README.md

# 查看开发规范
AGENTS.md

# 查看API文档
README.md（API文档部分）
```

### 使用DevToolkit

```bash
# 记录系统问题
python devtoolkit/src/devtoolkit/evolution.py add "问题描述"

# 查看错误记录
python devtoolkit/src/devtoolkit/evolution.py list

# 同步到全局
python devtoolkit/src/devtoolkit/bridge.py sync
```

---

## ✅ 启动检查清单

- [ ] Node.js >= 16.0 已安装
- [ ] MongoDB >= 4.4 已安装并启动
- [ ] 项目依赖已安装（npm install）
- [ ] 数据库已初始化（npm run init-db）
- [ ] 系统已启动（npm start）
- [ ] 可以访问 http://localhost:3000
- [ ] 可以使用 admin/admin123 登录
- [ ] 可以查看知识库（5条记录）
- [ ] 可以查看案例（3个案例）
- [ ] 可以完成考试（2套测试）

---

## 🎉 启动成功标志

如果看到以下内容，说明启动成功：

```
🎉 系统启动成功！
📱 访问地址: http://localhost:3000
📊 数据库: mongodb://127.0.0.1:27017/well_logging_training_memory
🤖 AI服务: 本地AI服务（规则引擎模式）
```

---

**启动完成！**

现在可以访问 http://localhost:3000 开始使用系统了。

有任何问题请查看：

- 📖 `docs/DevToolkit问题排查.md`
- 📖 `docs/培训材料准备-执行摘要.md`
- 📖 `data/content-templates.md`

祝使用愉快！🚀
