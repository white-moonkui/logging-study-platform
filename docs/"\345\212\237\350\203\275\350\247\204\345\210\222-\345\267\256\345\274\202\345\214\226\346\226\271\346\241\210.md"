# 测井专业智能培训系统 — 差异化功能方案

> 基于平台现状（Node.js + Express + 内存数据库 + 纯前端 + 石化通 MCP + EPBP 提取 + 测井行业 + 胜利油田内网环境）的独特功能规划。

---

## 总体设计理念

**学 → 练 → 考 → 错 → 复习 → 工作验证** 闭环。不复制通用 LMS，而是做"测井一线人员用得上、离不开"的工具。

---

## 功能矩阵

| 优先级 | 功能 | 独特指数 | 工作量 | 排期 |
|--------|------|----------|--------|------|
| **P0** | 错误驱动复习（SRS + 错误关联） | ⭐⭐⭐⭐⭐ | ~2天 | **第1期** |
| **P0** | 石化通学习推送 | ⭐⭐⭐⭐⭐ | ~2.5天 | **第1期** |
| **P1** | 交互决策树案例 | ⭐⭐⭐⭐ | ~2.5天 | 第2期 |
| **P1** | 岗位胜任力成长路径 | ⭐⭐⭐⭐ | ~2天 | 第2期 |
| **P2** | 知识图谱关联学习 | ⭐⭐⭐ | ~2天 | 第3期 |
| **P2** | 每日3分钟微学习 | ⭐⭐⭐ | ~1.5天 | 第3期 |
| **P3** | 党务+业务融合 | ⭐⭐⭐ | ~1天 | 第4期 |
| **P3** | 测井术语 flashcard | ⭐⭐ | ~1天 | 插空 |

---

## 一、错误驱动复习（Error-Driven SRS） [P0]

### 核心理念

从"学完就过"变为"错题自动进复习队列"。关联 devtoolkit 的 Error Evolution 和 EPBP 数据质量错误。

### 流程

```
考试做错题 ─→ 对应知识点标记为 "weak"
                    ↓
EPBP 提取到数据质量错误 ─→ 关联到操作规范知识点
                    ↓
          每日复习队列优先级 = f(遗忘曲线, 错误次数, 错误严重度)
                    ↓
用户答题 → SM-2 动态调整间隔: 1天→3天→7天→14天→1月
                    ↓
            掌握度达标 → 移出复习队列
```

### 需要新增的组件

1. **LearningProgress 模型扩展**: 加 `easeFactor`, `interval`, `reviewCount`, `nextReviewAt`, `retentionHistory[]`
2. **SM-2 算法工具函数**: `utils/srsAlgorithm.js` (~40行)
3. **三个新 API**: `/due-reviews`, `/:id/review`, `/review-stats`
4. **与 devtoolkit 联动**: 考试错题映射到知识点，生成复习记录

### 独特价值

通用 LMS 没有"实际生产数据→培训→复习"的闭环。EPBP 错误数据作输入是独家能力。

---

## 二、石化通学习推送 [P0] ← 详见独立方案

`docs/功能规划-石化通学习推送.md`

---

## 三、交互决策树案例（Branching Scenario）[P1]

### 核心理念

测井事故处理是经验学科。将隐性经验变为交互式决策树——"仪器卡在3200m，泵压异常升高，你怎么办？"

### 交互模式

```
案例描述 → [选择A: 上提钻具] → 后果: 电缆断，落井事故 ❌
          [选择B: 循环泥浆] → 后果: 压力恢复正常 ✅
                              ↓
                      [选择B1: 继续作业] → ...
                      [选择B2: 起出检查] → ...
```

### 实现路径

- **Case 模型扩展**: 加 `decisionTree` 字段（JSON 树结构）
  - 每节点: `{ prompt, options: [{ text, nextNodeId, feedback, consequence }], isEnd }`
- **前端组件**: 决策树渲染器（纯 JS 组件，渐进式展示节点）
- **AI 评估**: 用户完成后，本地 AI 引擎评估决策路径质量，给出评分和改进建议
- **回放功能**: 允许用户回看自己的决策路径，对比"专家路径"

### 独特价值

市面上没有测井行业的决策树培训工具。将老专家的经验数字化、可交互。

---

## 四、岗位胜任力成长路径 [P1]

### 核心理念

测井工程师有明确的职业序列，但每个级别"具体要掌握什么"没有系统化。

### 岗位链

```
实习生 → 操作员 → 技术员 → 工程师 → 高级工程师 → 专家
```

### 每个级别关联

- 必须掌握的知识模块（Knowledge + 考试）
- 必须完成的案例（Case + 决策树评分）
- 必须达到的考试准确率阈值
- 实操能力证明（通过模拟评分）

### 实现路径

1. **定义岗位能力矩阵**: JSON 配置文件 `data/competency_framework.json`
   - 每个岗位级别: `{ requiredKnowledge: [], requiredExams: [], requiredCases: [], minScores: {} }`
2. **前端"成长地图"**: 可视化道路图，每个节点标注"已掌握/进行中/未解锁"
3. **差距分析 API**: 对比用户当前进度 vs 目标岗位要求，自动推荐"下一步该学什么"
4. **打通能力评估**: `abilityMatrix` 五维能力图直接映射到岗位要求

### 独特价值

通用 LMS 的课程路径不适用于国企的技术职称体系。把职称晋升要求数字化、可视化，是很好的切入点。

---

## 五、知识图谱关联学习 [P2]

### 核心理念

测井概念不孤立。知识图谱而非扁平列表驱动学习路径。

### 数据结构

```
知识点A: 声波时差测井
├── 前置知识: 声波传播理论, 测井基本原理
├── 关联知识: 孔隙度计算, 岩性识别
├── 应用场景: 储层评价, 工程预警
└── 难度递进: 基础→标准→综合→案例
```

### 实现路径

1. **Category / Knowledge 模型扩展**: 加 `prerequisites: [ObjectId]`, `relatedTo: [ObjectId]` 字段
2. **图谱 API**: `/api/knowledge/graph/:id` 返回当前节点的前置/关联/后续知识
3. **前端可视化**: D3.js 力导向图渲染知识网络，点击节点跳转学习
4. **学习路径推导**: 当用户学完某个知识点后，图谱自动推荐"接下来可以学什么"

---

## 六、每日 3 分钟微学习 [P2]

### 核心理念

一线操作员没时间读长篇教材。每天 3 分钟能完成的学习单元。

### 内容格式

```
1 个概念卡片（30秒阅读）
+ 1 道自测题（1分钟作答）
+ 结果反馈 + 关联知识点跳转（1分钟）
```

### 结合点

- 天然适合 SRS 复习队列：学完后第 1/3/7/14/30 天各推送一次
- 石化通推送的"每日一题"就是微学习的最佳入口

---

## 七、党务+业务融合学习 [P3]

### 核心理念

测井监督站党支部，同一批人兼具党员和技术人员身份。

### 功能设计

- **党员技术攻关**: 将技术难度高的知识点标注为"党员攻关课题"
- **主题党日学习包**: 一键生成"时政学习 + 本周技术要点"组合包
- **融入现有流程**: 不新增模块，在已有党务系统中增加"技术学习"标签

### 独特价值

全中国没有 SaaS 培训平台做这事。但在你的场景中，党务和业务是同一批人，融合很自然。

---

## 八、测井术语 Flashcard [P3]

### 核心理念

测井大量术语来自英语（LWD, DTS, NMR, ECS...），新人常被卡住。

### 功能设计

- **术语词库**: JSON 文件 `data/glossary.json`，含中英文对照 + 解释 + 所属分类
- **Flashcard 模式**: 正面术语 → 背面解释，复用 SM-2 排期算法
- **考核模式**: 随机抽 10 个术语，答对率计入 abilityMatrix

---

## 推荐实施路线

### 第1期：核心闭环（~5天）

```
石化通推送 ←→ 错误驱动复习
    ↑                ↑
每天1题复习       错题自动入队
    ↓                ↓
回复即记录       SM-2动态排期
```

### 第2期：深度交互（~5天）

```
决策树案例 + 岗位胜任力路径
    ↑              ↑
经验数字化      能力可视化
```

### 第3期：生态扩展（~4天）

```
知识图谱 + 微学习
```

### 第4期：特色深化（~2天）

```
党务+业务融合 + 术语Flashcard
```

---

_生成时间: 2026-07-10_
_基于代码库 D:\AI-ITEM\logging-study-platform v1.1.0_
